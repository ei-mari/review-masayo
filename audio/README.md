# Audio Folders

Place each student's audio files in a separate folder.

- `/audio/masayo/`
- `/audio/tomoko/`

The current page config expects:

- Masayo: `ma_1.m4a` to `ma_10.m4a`
- Tomoko: add filenames in `app.js` when ready
